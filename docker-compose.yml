version: '3.9'

name: RTProAI

services:
  backend:
    image: rtpro-devmod:latest
    container_name: RTProAI-c
    working_dir: /home/app
    ports:
      - "8000:8000"
    volumes:
      - .:/home/app
    networks:
      - network
    stdin_open: true
    tty: true
    depends_on:
      - LLM
    
  LLM:
    image: ollama/ollama:0.11.5-rc4-rocm
    container_name: LLM-c
    networks:
      - network
    volumes:
      - ollama-models:/root/.ollama
    ports:
      - "11434:11434"
    restart: unless-stopped

networks:
  network:
    driver: bridge

volumes:
  ollama-models: